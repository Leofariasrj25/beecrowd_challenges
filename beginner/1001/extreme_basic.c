// challenge 1001
// objective read two variables and perform the sum of these two. store the result on a variable

#include <stdio.h>

int	main(int argc, char *argv[])
{
	int	a;
	int	b;
	int	x;

	scanf("%d %d", &a, &b);
	x = a + b;
	printf("X = %d\n", x);
	return (0);
}
